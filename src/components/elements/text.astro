---
interface Props {
  size?: "md" | "lg";
  class?: string;
}

const { size = "md", class: className } = Astro.props;
---

<div
  class:list={[
    size === "md" && "text-base/7",
    size === "lg" && "text-lg/8",
    "text-olive-700 dark:text-olive-400",
    className,
  ]}
>
  <slot />
</div>
