---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
const links = await Astro.slots.render("links");
---

<header id={id} class:list={["sticky top-0 z-10 bg-olive-100 dark:bg-olive-950", className]}>
  <style is:inline>
    :root {
      --scroll-padding-top: 5.25rem;
    }
  </style>
  <nav>
    <div class="mx-auto flex h-(--scroll-padding-top) max-w-7xl items-center gap-4 px-6 lg:px-10">
      <div class="flex flex-1 items-center">
        <slot name="logo" />
      </div>
      <div class="flex flex-1 items-center justify-end gap-4">
        <div class="flex shrink-0 items-center gap-5">
          <div class="hidden lg:contents" set:html={links} />
          <slot name="actions" />
        </div>

        <button
          command="show-modal"
          commandfor="mobile-menu"
          aria-label="Toggle menu"
          class="inline-flex rounded-full p-1.5 text-olive-950 hover:bg-olive-950/10 lg:hidden dark:text-white dark:hover:bg-white/10"
        >
          <svg viewBox="0 0 24 24" fill="currentColor" class="size-6">
            <path
              fill-rule="evenodd"
              d="M3.748 8.248a.75.75 0 0 1 .75-.75h15a.75.75 0 0 1 0 1.5h-15a.75.75 0 0 1-.75-.75ZM3.748 15.75a.75.75 0 0 1 .75-.751h15a.75.75 0 0 1 0 1.5h-15a.75.75 0 0 1-.75-.75Z"
              clip-rule="evenodd"></path>
          </svg>
        </button>
      </div>
    </div>

    <dialog id="mobile-menu" class="backdrop:bg-transparent lg:hidden">
      <div class="fixed inset-0 bg-olive-100 px-6 py-6 lg:px-10 dark:bg-olive-950">
        <div class="flex justify-end">
          <button
            command="close"
            commandfor="mobile-menu"
            aria-label="Toggle menu"
            class="inline-flex rounded-full p-1.5 text-olive-950 hover:bg-olive-950/10 dark:text-white dark:hover:bg-white/10"
          >
            <svg fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <div class="mt-6 flex flex-col gap-6" set:html={links} />
      </div>
    </dialog>
  </nav>
</header>
